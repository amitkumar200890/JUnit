-> JUnit API is called Jupiter.
-> Vintage is another JUnit API used for testing older codes.
-> Extension (EXT - 3rd party) is used when we write our own extension and use the JUnit platform to test.

For JUnit 5 we need to add 2 dependencies in maven i.e Jupiter and Platform.


Jupiter JUnit:
New programming model in JUnit
Extension model
What we will be working with

Structure:
Project:
	src
	    main
		java
		      io.javabrains(package)
					MathUtils.java

	src
	    test
		java
		     io.javabrains(package)
					MathUtilsTest.java

	pom.xml
